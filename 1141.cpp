#include <iostream>

using namespace std;
 
int main() {	
	// Valor inserido
	int count;
	cin >> count; 
	while(valor != '0') {
		string strs[valor];
		for(int i = 0 ; i < valor ;  i++) {
			cin >> strs[i];
			cin.ignore();
			getline(cin, strs[i]);
		}
		
		for(int i = 0 ; i < valor ;  i++) {
			// Find you sequences
		}
		
		cin >> count;
	}
	return 0;
}
